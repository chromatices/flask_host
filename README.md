# rest_host
rest host for test

### Usage
- shell(local, recommanded)
```shell
python main.py
```

- docker
```docker
docker build . -t [tagname:version]
docker run --rm -it [tagname:version] /bin/bash
```